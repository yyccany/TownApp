# 2026-06-27 工作日志

## TownApp 架构导航改造 + 视觉统一 + 详情页连续阅读 + 微交互

### 完成内容（5大优先级全部落地，编译通过，APK生成成功）

**P1 架构导航改造**
- 移除底部双Tab导航栏（`TownBottomNavigation`不再使用）
- 新建 `AppDrawer.kt`：`ModalNavigationDrawer` 侧边抽屉，收纳6项低频功能（收藏/笔记/导入导出/反馈/关于/设置）
- 新建 `StandardTopBar.kt`：标准化顶部Header（左汉堡/中标题/右视图切换+搜索），全页面统一
- 重写 `MainActivity.kt`：DrawerState协程管理，搜索面板覆盖层在顶层渲染
- 底部预留40dp安全留白（`DictionaryTokens.bottomSafePadding`）

**P2 首页布局重构**
- 重写 `DictionaryHomeScreen.kt`：横向可滑动分类Tab栏（9个Tab：今日认知+7分类+值得先读）
- 删除分类网格卡片、独立搜索框、DictionaryHeader
- 全屏词条列表，点击Tab直接刷新对应分类，无二次跳转
- 视图切换：卡片视图 / 紧凑列表视图
- 搜索改为右上角图标 → 全屏搜索覆盖面板（`SearchOverlay`）

**P3 全局视觉细节统一**
- `Theme.kt` 新增 `DictionaryTokens.Type` 四层文字规范（title/heading/body/caption）
- 卡片16dp圆角、标签8dp圆角统一；柔和向下阴影
- 页面左右24dp边距，模块间距16/24dp
- 折叠按钮改为「文字+箭头」组合（收起∧/展开∨）
- 侧边竖条/序号底色/标签色系严格对应分类与毒性等级

**P4 详情页连续阅读**
- `IdiomCriticScreen.kt` 底部新增 `ReadingNavigation`：上一篇/下一篇（同分类内循环切换）+ 随机认知词条（全库随机）
- 切换词条时 `LaunchedEffect(idiom.id)` 滚动回顶部
- 随机词条打破顺序浏览，贴合「反背诵」产品内核

**P5 加分微交互**
- 首页Header底部弧形裁切凹陷动效（`drawBehind` + Path，scrollState驱动深度）
- 卡片点击缩放回弹（`clickableWithScale`，spring动画）
- 模块展开/收起平滑高度过渡（`AnimatedVisibility` + `expandVertically`/`shrinkVertically`）

### 新建文件
- `ui/components/StandardTopBar.kt` — 标准化顶部导航栏 + SearchIcon + ViewToggleIcon
- `ui/components/AppDrawer.kt` — 侧边抽屉内容面板 + DrawerItem数据

### 修改文件
- `MainActivity.kt` — 全面重构（ModalNavigationDrawer + 搜索覆盖层）
- `ui/screens/DictionaryHomeScreen.kt` — 全面重构（横向Tab + 全屏列表）
- `ui/screens/IdiomCriticScreen.kt` — 重构（StandardTopBar + 连续阅读导航 + 统一Typography）
- `ui/screens/SettingsScreen.kt` — 重构（StandardTopBar + DictionaryTokens）
- `ui/Theme.kt` — DictionaryTokens新增Type对象/shadowColor/bottomSafePadding

### 保留不用的旧文件（未删除，无引用）
- `ui/components/TownNavigation.kt`（底部导航，已无引用）
- `ui/components/TopNavBar.kt`（旧顶部栏，已无引用）

---

## 二轮整改：Tab栏色彩系统 + 图标线条统一 + 详情页细节打磨（20:56）

**Tab栏色彩系统重构（DictionaryHomeScreen.kt + Theme.kt）**
- Theme.kt 新增 `DictionaryTokens.TabBar` 对象，定义5组分类色条色彩：
  - 人际关系/亲密关系→淡粉, 人格品德→浅暖棕, 处事方法/小镇内在→青柠绿
  - 社会观念→浅土金, 认知思维→浅灰蓝, 今日认知→暖中性, 值得先读→特征绿
- 选中态统一低饱和青柠绿填充+白字，所有分类选中色完全统一
- 未选中Tab：纯白底+左侧3dp窄分类色条+轻拟态shadow
- Tab文字：选中Bold深色 / 未选中Normal浅灰弱化
- Tab 8dp圆角 + 左右14dp加宽留白 + 柔和阴影浮起

**全局图标线条统一（StandardTopBar.kt + IdiomCriticScreen.kt）**
- NavIcon汉堡/返回：2f → 1.6f
- SearchIcon搜索：1.8f → 1.6f  
- MiniStarIcon星标：1.4f → 1.6f
- 全应用图标线条统一为1.6f density stroke

**首页排版韵律**
- Tab栏与词条列表间距：12dp → 24dp缓冲

**详情页细节优化（IdiomCriticScreen.kt）**
- 阅读导航Column间距：12dp → 10dp（随机词条区与上/下方间距收紧）
- 底部标语（词语只是框架…）：13sp → 14sp，caption → body色（提亮）
- 折叠按钮8dp圆角浅底色容器（已有，确认OK）
- 上/下一篇卡片词条名Bold+textTitle（已有，确认OK）

---

## 三轮整改：品牌更名Vera + 三视图统一 + Tab图标 + 全局规范 + 细节加分（21:45）

**Phase 1: 品牌更名 Vera + 抽屉改造（AppDrawer.kt）**
- 抽屉头部改为深色横幅（#1E1E1E）：圆形Vera Logo（青柠绿底+白色V）+ 大号"Vera"英文名 + 副标题"Cognition Dictionary · Train Your Judgment"
- 菜单项布局改为文字左对齐+图标右对齐（栅格强制对齐）
- 版本号"v3.0"收纳在「关于小镇」行右侧，底部独立版本信息删除
- 首页标题「万物薪俸小镇」→「Vera」
- SettingsScreen/SettingsScreen关于页 →「Vera」

**Phase 2: 三种视图统一卡片外壳（DictionaryHomeScreen.kt）**
- `IdiomCompactItem` 加Card包裹（16dp圆角+elevationCard阴影），与IdiomCardItem/SearchResultItem完全统一
- 三视图统一参数：左侧竖条4dp / 内边距start=20dp end=24dp top=14-20dp / IntrinsicSize.Min高度
- 竖条颜色统一为 categoryDarkColor（分类色系）
- 紧凑视图仅隐藏简介文字，卡片外壳全程不变

**Phase 3: Tab栏分类线性图标（DictionaryHomeScreen.kt）**
- HomeTab新增iconType字段，9个Tab各配极简线性图标（Canvas绘制，1.6f stroke）
- sun/person/tool/people/globe/heart/brain/home/star 九种图标类型
- 图标在TabChip中显示（分类色条→图标→文字），选中态图标变白色

**Phase 4: 全局设计规范强制检查**
- SettingsScreen品牌名修正为Vera
- 全页面圆角16dp/8dp双标准+四层文字层级确认合规

**Phase 5: 搜索页+细节优化**
- 搜索结果列表间距：12dp → sectionSpacing(16dp)
- 热门词条列表间距：12dp → sectionSpacing(16dp)

---

## 四轮改动：更新Vera品牌图标为圆圈VERA+微笑弧线（22:09）

**AppDrawer.kt**
- VeraLogoIcon 新建，替换原先简单的青柠绿圆形V：
  - Canvas绘制白色圆圈轮廓（1.8f stroke）
  - Text层叠「VERA」文字（12sp Bold 白色，letterSpacing=1.2sp）
  - Canvas绘制微笑弧线（青柠绿 #A3D98B，圆圈下方，0°→180° arc）
  - Box层叠布局：Canvas(圆圈+弧线) + Text(VERA) 叠加于72dp容器内
- 新增 `fillMaxSize` import

---

## 五轮改动：桌面图标+抽屉品牌统一+首页列表精简（22:30）

**P0: 桌面App图标（白底黑线Vera）**
- `res/drawable/ic_launcher_background.xml` → 纯白 #FFFFFF
- `res/drawable/ic_launcher_foreground.xml` → 矢量：圆圈轮廓 + VERAA简化路径 + 微笑弧线（全 #1E1E1E 黑色，1.4-1.8f stroke）
- 所有密度 mipmap-{mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi} 生成 PNG 回退图标（Pillow绘制，48/72/96/144/192px）
- 删除旧 `.webp` 文件避免重复资源冲突
- `strings.xml` app_name: "Town app" → "Vera"

**P1: 侧边抽屉品牌统一（AppDrawer.kt）**
- VeraLogoIcon 微笑弧线色彩：青柠绿 #A3D98B → 纯白（与圆圈、字母统一）
- 菜单文案：「关于小镇」→「关于 VERA 认知字典」
- 帮助文案：「帮助小镇变得更好」→「帮助 Vera 变得更好」

**P2: 首页列表排版规整化（DictionaryHomeScreen.kt）**
- **移除所有卡片左侧彩色竖条**：IdiomCardItem/IdiomCompactItem/SearchResultItem 删除 4dp 分类色条 Box
- 三视图统一采用 Column+padding 布局，标题 weight(1f, fill=false) + 标签固定在右侧
- 删除 `accentColor` 未使用变量
- **Tab栏纯文字高亮模式**：TabChip 删除分类色条、绿色填充背景、阴影
  - 选中态：Bold 深色文字 + 深色图标（无背景色填充）
  - 未选中态：Normal 灰色文字 + 灰色图标
  - 全白底极简风格，8dp 圆角

---

## 六轮改动：全局视觉规范 + 已读置灰 + Tab下划线 + 分割线组件（22:40）

**全局视觉规范（Theme.kt）**
- 页面底色：`#F9F6ED` → `#F8F5EB`（低饱和混米白）
- 文字色值：textTitle `#2D2D2D`→`#121212`, textBody `#666666`→`#707070`, textCaption `#999999`→`#8C8C8C`
- 已读色值体系：textTitleUnread=#121212, textTitleRead=#8C8C8C, textBodyUnread=#707070, textBodyRead=#8C8C8C
- 图标线条基准：iconStrokeDensity=2f（Canvas stroke 统一从 1.6f→2f）
- 文字层级：titleSize 22sp→18sp(Medium), bodySize 15sp→14sp, tagSize 11sp→12sp

**全局图标 stroke 2f 统一**（StandardTopBar / AppDrawer / DictionaryHomeScreen / IdiomCriticScreen）
- NavIcon / SearchIcon / ViewToggleIcon: 1.6f→2f
- VeraLogoIcon: 1.8f→2f
- DrawerIcon: 1.6f→2f
- TabCategoryIcon: 1.6f→2f
- MiniCheckIcon / MiniStarIcon / RandomIcon: 1.6f→2f
- FavoriteIcon（新增）: 2f

**已读置灰体系（MainActivity + DictionaryHomeScreen）**
- MainActivity: `readIdiomIds = mutableStateListOf<String>()`，`onNavigateToIdiom` 中自动标记已读
- IdiomCardItem/IdiomCompactItem: 新增 `isRead` 参数，标题/简介/标签根据已读状态切换色值+透明度
  - 未读标题=#121212 → 已读=#8C8C8C
  - 标签背景和文字：已读时 alpha=0.5f 降透明

**Tab 下划线高亮模式（DictionaryHomeScreen.kt）**
- TabChip 从圆角按钮→纯文字+底部2dp分类色条下划线
- 选中：Bold深色+底部色条；未选中：Normal浅灰+透明底部
- Tab间距：8dp→2dp，padding: 24dp→16dp bottom

**卡片内边距收紧（DictionaryHomeScreen.kt）**
- IdiomCardItem/IdiomCompactItem/SearchResultItem padding: 20/24/18dp → 16/16/14dp
- 标题-简介间距：维持 4dp

**分割线信息流组件（DividerLineList.kt 新建）**
- 0.5dp细分割线分隔条目，无卡片/无圆角/无阴影
- 左：18-22sp Medium标题+13sp简介；右：独立收藏按钮（互不干扰）
- 下划线高亮Tab栏（无背景）
- 数据模型：DividerLineItem(id/title/subtitle/isFavorited), DividerLineTab

**详情页收紧（IdiomCriticScreen.kt）**
- ContentCard 内部padding: cardPadding(16dp)→12dp
- 全局 icon stroke 已统一 2f

---
## 2026-06-28 内容层+UI交互层双重重构（17:56）

**内容层：townPerspective五层分段重构（idioms.json）**
- Python脚本批量处理164条词条，157条包含①标记的全部重构
- townPerspective五层视角（①唯物史观②社会批判③心理学④科学⑤情感）各自独立段落，段落间\n\n空行分隔
- 解决了"一整段长文本堆砌"问题，内容层级清晰可辨

**UI层：CollapsibleSection交互重构（IdiomCriticScreen.kt）**
- 废除标题栏右侧"已展开/已收起"状态文字，标题栏只展示标题
- 折叠预览高度：80dp→72dp（精确对应14sp正文+22sp行高×3行=66dp+留白）
- 渐隐遮罩高度：48dp→40dp，底部融合卡片底色（surface）更自然
- 底部操作按钮：折叠态居中【展开全部↓】Button（primaryLight 18%透明度底+primary文字），展开态右对齐【收起↑】TextButton
- 按钮圆角统一DictionaryTokens.radiusButton(8dp)

**UI层：核心启示改标准ContentCard**
- 原手写Card改为ContentCard(backgroundColor = cardBg)包裹
- 标题统一为DictionaryTokens.Type.headingSize(17sp Bold)，与模块标题规范一致
- 正文用DictionaryTokens.Type.bodySize(14sp) + bodyLineHeight(22sp)，全局统一

**UI层：spotlights拆为独立卡片**
- 从"小镇辩证解读"卡片内拆分出来，成为独立ContentCard(backgroundColor = primaryBg)
- 标题"警示提示"用primary色+heading规范，与解读卡片视觉分离
- spotlights始终展开（不折叠），作为独立辅助提示区块

**UI层：全局排版常量统一**
- ExampleItem行高：22.sp→DictionaryTokens.Type.bodyLineHeight（22.sp，常量统一管理）
- 小镇辩证解读Text行高：22.sp→DictionaryTokens.Type.bodyLineHeight
- 所有ContentCard内Text组件统一引用DictionaryTokens.Type常量

**编译结果**
- BUILD SUCCESSFUL in 10s，无语法错误
- APK路径：app/build/outputs/apk/debug/app-debug.apk

**待优化**
- 123条词条缺失④自然科学视角和⑤情感视角（内容补齐P2优先级）
- spotlights独立卡片样式可进一步优化（当前是plain Column，可加左侧绿色竖条或星标图标装饰）

---

## 2026-06-28 最终精简修改：序号/标识/布局重构（18:19）

**一、五层视角序号空心圆圈（IdiomCriticScreen.kt）**
- 新增 `CircleNumber` 组件：Canvas空心圆圈（1.6f stroke）+ 居中Text数字（11sp Medium），统一20dp尺寸
- 新增 `TownPerspectiveContent` 组件：按①②③④⑤正则拆分townPerspective文本，移除Unicode标记字符，每段渲染为 `Row[CircleNumber + Text]`，段落间距14dp
- 文字左对齐，线条粗细统一为1.6f density，全程纯黑 #2D2A26

**二、现实案例标识改用两种单色单线符号**
- 新增 `HollowCheckIcon`：空心单线对勾（1.8f stroke），黑色 #2D2A26，表示原生合理场景
- 新增 `HollowWarningIcon`：空心单线警示三角+感叹号（1.8f stroke），黑色 #2D2A26，表示现代滥用场景
- 新增 `isAbuseScenario()` 检测函数：14个关键词（现代/当代/职场/组织/企业/剥削/内卷/规训等）
- ExampleItem 移除 iconColor 参数，根据场景自动切换图标

**三、警示提示合并回小镇辩证解读模块**
- 新增 `SpotlightsBlock` 组件：嵌入core卡片CollapsibleSection内部末尾
- 浅绿底色（primary.copy(alpha=0.1f)）圆角区块 + 星标图标 + "警示提示"标签
- 内容左缩进对齐星标，caption字号（13sp），消除外部独立卡片碎片化感

**四、交互逻辑确认（已存在）**
- 全局三种展开模式（0=智能预览/1=全部展开/2=全部收起）—— 已在CollapsibleSection中实现
- 折叠态：72dp预览 + 渐隐遮罩 + 底部按钮，无"已展开/已收起"状态文字
- DictionaryTokens.Type常量统一全局排版

**编译结果**
- BUILD SUCCESSFUL in 25s，APK已生成

---

## 2026-06-28 视觉统一性大重构——四大核心病根治理（18:28）

**一、统一交互：全部卡片仅底部独立圆角单色按钮**
- 废除两套按钮形态（折叠态居中浅绿Button + 展开态右下角纯文字TextButton）
- 改为唯一居中OutlinedButton：1dp半透明描边（#2D2A26 alpha 0.25）+ 8dp圆角，折叠态"展开全部"展开态"收起"同位置同风格
- 右上角全局切换：删除浅绿色TextButton"全部展开/全部收起"，改为40dp黑色单线IconButton（`ExpandCollapseIcon`：Canvas双箭头图标）

**二、统一符号：序号、对勾、三角、警示全部纯黑空心单线**
- 序号圆圈：20dp→18dp，确保内嵌卡片内部不凸出
- 警示提示：去掉绿色五角星（删除MiniStarIcon）和浅绿底色区块
  - 改为空心圆点（6dp Canvas circle stroke）+ 无底色纯文字 + 顶部1dp分割线
- 删除废弃的MiniCheckIcon
- HollowCheckIcon/HollowWarningIcon确认均为空心单线（drawLine/drawPath+Stroke）

**三、统一底色：全部ContentCard改为纯白**
- 移除cardExample(暖米黄)、cardCore(青柠绿)、cardTip(浅青灰)三色底色
- 全部使用cardBg(#FFFFFF)，消除页面斑驳多色碎片化

**四、统一排版：头部标签+底部导航+全局细节**
- 头部标签：去掉彩色圆角色块背景，改为无底色13sp灰色纯文字
- 认知偏差标签：去掉灰底圆角块，改为"#tag"纯文字可点击
- 底部"继续阅读"：分隔线增强留白（上下各4dp）+ 颜色加深（alpha 0.12）
- 底部标语：14sp→15sp
- 卡片间距统一为sectionSpacing(16dp)
- DetailHeader清理未使用变量tagColor/darkColor

**编译结果**
- BUILD SUCCESSFUL in 25s，0错误，仅3个pre-existing onMenuClick未使用警告