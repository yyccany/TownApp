# 2026-06-28

## 序号外凸 + 对勾空心化修复
- **CollapsibleSection 渐变遮罩定位修复**：将渐隐遮罩 Box 从 clipped Box 外部移入内部。之前 `.align(Alignment.BottomCenter)` 在无固定高度的父 Box 中不生效，导致序号①②③穿透渐变露出卡片边界。修复后遮罩在 72dp 固定高度 Box 内正确置底。
- **HollowCheckIcon 改为单路径描边**：原先用两段 `drawLine` + Round Cap，在交汇处两个圆头叠加重合产生实心感。改用 `Path` + `Stroke(join=StrokeJoin.Round)` 单次描边，彻底空心化。
- 验证：文件中无残留 `Color(0xFF...)` 硬编码，VeraColorScheme 已全覆盖。
- BUILD SUCCESSFUL

## 完全移除数字序号①②③（方案1：纯分段无序号）
- **JSON 数据层**：Python 脚本批量清理 164 个词条 `townPerspective` 字段中的 ①②③④⑤ 字符，保留 `\n\n` 作为段间自然分隔。验证无残留。
- **UI 渲染层**：`TownPerspectiveContent` 从 Row+CircleNumber 布局重建为纯 Column(Text+Spacer) 竖排分段。按 `\n\n` 拆分段落，段间距 16dp。
- **死代码清理**：删除 `CircleNumber` composable（18dp 空心圆+数字 Canvas 组件）。
- **主题色板不变**：`VeraColorScheme.iconCircleNumber` 保留但不再被引用。
- BUILD SUCCESSFUL

## 方案A：统一空心单线图标 + 全局对齐排版（兼容双主题）
- **统一布局组件 `IconParagraph`**：图标 14dp 左对齐 + 10dp 缩进 + body 正文 weight(1f) 自动换行。现实案例与辩证解读共用同一套对齐参数，彻底消除排版差异。
- **辩证解读三大项目符号（全部归入 `iconLine` 语义色）**：
  - ○ `HollowCircleBullet` → 唯物史观视角（宏观客观）
  - □ `HollowSquareBullet` → 现代社会结构批判（规则/组织）
  - ◇ `HollowDiamondBullet` → 认知心理学视角（内心思辨）
- **`TownPerspectiveContent` 智能图标匹配**：按段落前缀 (`startsWith("唯物史观")` / `"现代社会结构批判"` / `"认知心理学"`) 自动分配对应图标，fallback → ○。
- **`ExampleItem` 重构**：改用 `IconParagraph` 布局，对勾/警示三角仅作为 `icon` 参数传入，布局逻辑不再重复。
- **双主题兼容**：三类新图标统一读取 `LocalVeraColors.current.iconLine`，黑白版全黑 #000，马卡龙版灰蓝 #8AA8C0。图标外形/位置/排版完全不变。
- 新增 import `androidx.compose.ui.geometry.Size`。
- BUILD SUCCESSFUL

## 马卡龙童趣版四阶段落地确认
- **阶段1 数据层** ✓：`UserPreferencesManager.getThemeMode()` 返回 `Flow<VeraThemeMode>`，默认 `DefaultMonochrome`；`setThemeMode()` 写入 DataStore；`MainActivity` 通过 `collectAsState` + `CompositionLocalProvider(LocalVeraColors provides ...)` 全局注入。
- **阶段2 切换入口** ✓：`SettingsScreen`「外观主题」卡片含 "默认黑白极简" / "马卡龙童趣彩色" 双 RadioButton，点击即时写持久化 + 触发重组。
- **阶段3 色值替换** ✓：`IdiomCriticScreen.kt` 零残留 `Color(0xFF...)` 硬编码。14 个 VeraThemeColors 字段全部有对应引用点（除 `tagBg`/`tagText`/`iconCircleNumber` 预留给未来），所有图标/按钮/警示/随机卡片/笔记均由 `LocalVeraColors.current.*` 驱动。
- **阶段4 联调** ✓：BUILD SUCCESSFUL。两套主题共用一个 VeraThemeColors 实例注入链路，切换无排版位移风险。

## 《摊牌忍者》核心观点整合进"忍一时风平浪静"词条
- **选词依据**：用户指出"摊牌忍者"非成语，不适合做词条标题。最贴切的宿主词条是"忍一时风平浪静"——已有"积怨三年后一次爆发"的薄案例，正好被《摊牌忍者》深化。
- **6 处字段更新**：
  1. `townPerspective` 认知心理学层：新增"憋到总摊牌"行为链（180度翻转→清算全部罪状→终身监禁vs冒死逃生）+ 责任归属（搞到要摊牌第一责任在自己）+ 解决方向（先为总摊牌模式本身致歉）
  2. `distortedTruth`：新增"归因谬误"——把"自己选择憋着"的责任全然归咎于对方的"冷漠"
  3. `examples`：扩写积怨爆发案例，从薄薄一句变为完整行为链描述
  4. `actionableTip`：新增"为总摊牌模式本身先道歉"方案，用"可以尝试"开头，符合内容规范无命令式要求
  5. `spotlights`：新增 `[INTERPERSONAL]` "憋到憋不住再总摊牌，第一责任在自己"（w=0.85）
  6. `cognitiveBiasTags`：新增"归因谬误"（原仅有"非黑即白思维"）
- BUILD SUCCESSFUL

## 新增「主体性工具」独立Tab + 首条目「被说犟」
- **背景**：Vera 原只有"解构型"内容（成语→拆解被扭曲的真相），缺"建构型"模块（社交困境→认知工具）。用户选择方式B（重量方案）新增独立Tab。
- **新增数据模型** `data/subjectivity/SubjectivityTool.kt`：
  - `SubjectivityTool` data class（10字段：title/subtitle/labelMechanism/paradigmShift/responsibilityAnalysis/keyInsight/scenarios/actionFramework/relatedTags）
  - `SubjectivityToolLibrary` 单例，从 `assets/subjectivity/tools.json` 加载，API 与 `IdiomCritiqueLibrary` 一致
- **新增数据文件** `assets/subjectivity/tools.json`：首条目「被说犟」，整合了之前对话中关于标签识别/提问范式/责任分离的完整分析
- **新增详情页** `ui/screens/SubjectivityToolScreen.kt`：
  - 6个卡片区块：标题区→标签识别(○)→提问范式(□)→责任分离(◇)→现实场景(✓/▲)→行动框架→核心洞见(灯泡高亮)→关联标签
  - 全部图标使用 VeraColorScheme 语义色，统一 1.8f stroke 空心单线风格
  - 自包含 `SubjectivityIcon` 图标集（circle/square/diamond/check/warning/lightbulb），不复用 IdiomCriticScreen 的私有组件
- **首页改造** `DictionaryHomeScreen.kt`：
  - `HomeTab` 新增 `isSubjectivity` 标志位
  - `homeTabs` 新增 `"subjectivity"` Tab（指南针图标 compass），排在"值得先读"之前
  - 选中主体性Tab时，加载 `SubjectivityToolLibrary.getAllTools()` 并渲染 `SubjectivityToolCardItem`
  - `TabCategoryIcon` 新增 compass 图标（圆+菱形指针）
- **导航改造** `MainActivity.kt`：
  - `onCreate()` 中同步初始化 `SubjectivityToolLibrary`
  - 新增 `selectedSubjectivityToolId` 状态，与 `selectedIdiomId` 互斥清除
  - `when` 块优先级：subjectivityTool > idiom > home
- **Theme.kt**：`tabBarColor("subjectivity")` → `TabBar.grayBlue`
- 编译修复：smart cast（用 `val toolId = selectedSubjectivityToolId!!`）+ FlowRow `@OptIn(ExperimentalLayoutApi::class)`
- BUILD SUCCESSFUL

## 统一分析引擎：主体性工具合并回成语数据模型 + entryType标记
- **理论决策**：用户指出三层分析框架（唯物史观→社会结构批判→认知心理学）本身就是唯物辩证法的标准落地，无论入口是成语/自造概念/学术名词/现代观念/社交困境，分析引擎完全复用，不需要两套独立体系。
- **EntryType 枚举**：`IdiomCritiqueLibrary.kt` 新增 `EntryType(displayName)` 枚举（IDIOM/COINED/ACADEMIC/MODERN_BELIEF/DILEMMA），`IdiomCritique` data class 新增 `entryType` 字段，默认 `IDIOM`。
- **IdiomAssetLoader** 新增 `entryType` 解析（`json.optString("entryType", "IDIOM")`）。
- **Library 新增** `getIdiomsByEntryType(entryType)` 筛选方法。
- **批量标记 164 条现有词条**：
  - IDIOM: 103（传统成语/俗语/谚语）
  - COINED: 16（自造概念词：面子囚徒、信息井蛙等）
  - ACADEMIC: 13（学术/心理学概念：沉没成本、锚定效应等）
  - MODERN_BELIEF: 32（现代观念陈述：学历决定一切等）
- **新增「被说犟」词条**（entryType=DILEMMA）：用三层唯物辩证结构重写，traditionalMeaning→标签生成机制，distortedTruth→归因谬误+内化标签，townPerspective→三层分析（唯物史观：等级社会权力工具 / 社会结构批判：残余等级压制 / 认知心理学：归因偏差+提问范式转换）。
- **DictionaryHomeScreen 统一**：「主体性工具」Tab 改为 `IdiomCritiqueLibrary.getIdiomsByEntryType(EntryType.DILEMMA)`，卡片渲染走统一 `IdiomCardItem`/`IdiomCompactItem`，移除 `SubjectivityToolCardItem`、`onNavigateToSubjectivityTool` 参数、`toolsToShow`。
- **MainActivity 统一**：移除 `SubjectivityToolLibrary.initialize`、`selectedSubjectivityToolId`、`onNavigateToSubjectivityTool`、`SubjectivityToolScreen` 路由分支。所有详情页统一走 `IdiomCriticScreen`。
- **删除文件**：`SubjectivityTool.kt` / `tools.json` / `SubjectivityToolScreen.kt`（含目录）。
- BUILD SUCCESSFUL，APK 11.4MB，165 条词条数据完整。

## 批量扩充词条 165→208（+43条）
- **DILEMMA +12条**：被催婚、被比较、被说自私、不好意思拒绝、被道德绑架、被说想太多、被说太敏感、被要求懂事、被说没上进心、被说太计较、被说变了、被要求感恩。DILEMMA从1条→13条，主体性工具Tab终于有实质内容。
- **ACADEMIC +7条**：禀赋效应、自利偏差、光环效应、刻板印象、旁观者效应、自我实现预言、选择悖论。补齐高频认知偏差学术概念。
- **MODERN_BELIEF +10条**：朋友圈比较焦虑、短视频知识错觉、精致穷、赢在起跑线、副业刚需、仪式感消费、对自己好=买买买、多任务高效误区、内向=社交差、不发朋友圈=没朋友。补齐信息时代流行观念。
- **IDIOM +10条**：不听老人言、枪打出头鸟、人善被人欺、船到桥头自然直、木秀于林风必摧之、好马不吃回头草、识时务者为俊杰、人挪活树挪死、善有善报恶有恶报、病急乱投医。
- **COINED +4条**：努力通胀、选择窒息、人设固化、情绪负债。Vera原创概念。
- 每条严格遵循7字段模板+三层唯物辩证结构（唯物史观/社会结构批判/认知心理学），通过字段完整性验证。
- 脚本位于 `scripts/add_*.py` + `scripts/merge_all_entries.py`，可重复执行。
- BUILD SUCCESSFUL，APK 11.7MB，208条词条字段完整性全部通过。
- 最终分布：IDIOM 113 / MODERN_BELIEF 42 / COINED 20 / ACADEMIC 20 / DILEMMA 13。

## 批量扩充词条 208→229（+21条 商业人格话术）
- 用户提出四组现代两字商业概念清单，并从马克思主义三层逻辑逐一论证适配性（唯物史观/社会结构批判/认知心理学），确认完全适配底层框架。
- **MODERN_BELIEF +21条**：
  - 商系四件套：智商、情商、财商、逆商
  - 人格品质类：勇气、自律、钝感力、格局、内核稳定、松弛感、高敏感、懂事、成熟
  - 消费成长话术：延迟满足、吃苦、内卷、躺平（精致穷/仪式感消费/赢在起跑线已存在跳过）
  - 职场规训类：抗压、会来事、执行力、情绪稳定
- 每条严格遵循7字段模板+三层唯物辩证结构，核心方法论：扬弃——承认客观能力差异存在，批判资本放大标准用于收割/掩盖结构性问题。
- 脚本：`scripts/add_commercial_concepts.py`（单引号字符串+q()函数替换中文全角引号，避免Python引号冲突）。
- BUILD SUCCESSFUL，APK 11.8MB，229条字段完整性全部通过。
- 最终分布：IDIOM 113 / MODERN_BELIEF 63 / COINED 20 / ACADEMIC 20 / DILEMMA 13。

## 批量扩充词条 229→271（+42条 六大类体系化扩充）
- 用户提供六大类概念清单，每类附马克思主义三层适配论证+录入策略（通用词条概括+独立高频词条），确认全部共用三层唯物辩证分析引擎。
- **Cat1 资本职场商业 +9条**（COINED 1 + MODERN_BELIEF 8）：现代人格话术的商品化异化(COINED概括)/资本主义/资本/职场/偶像崇拜/慕强/向上管理/职业伦理/奢侈品
- **Cat2 宏观社科 +6条**（MODERN_BELIEF）：生产关系/意识形态/权力与权利/全球化/社会伦理/社会化
- **Cat3 人际亲密关系 +9条**（DILEMMA 4 + MODERN_BELIEF 5）：校园霸凌/被冒犯/自卑/讨好(DILEMMA) + 亲密关系/亲子教育/人际交往/人己权界/自我认知(MODERN_BELIEF)
- **Cat4 劳动科技产业 +7条**（COINED 1 + MODERN_BELIEF 6）：市场经济下劳动与技术异化(COINED概括)/人工智能/现代化/现代性/投资/消费观/生产工具
- **Cat5 心理哲学道德 +9条**（ACADEMIC 4 + IDIOM 5）：祛魅/认知/判断力/心理学(ACADEMIC) + 善良/善恶/美德/勤俭/忠义(IDIOM)
- **Cat6 文化艺术 +2条**（MODERN_BELIEF）：审美/艺术
- 每条严格遵循7字段模板+三层唯物辩证结构。生成脚本分三批：`scripts/add_cat1_cat2.py`/`add_cat3_cat4.py`/`add_cat5_cat6.py`，生成JSON中间文件后合并。
- BUILD SUCCESSFUL，APK 11.8MB，271条字段完整性全部通过。
- 最终分布：IDIOM 118 / MODERN_BELIEF 90 / COINED 22 / ACADEMIC 24 / DILEMMA 17。

## 批量扩充词条 271→350（+79条 六大类全覆盖补齐）
- 用户再次列出六大类完整清单（130词），核对后43条已存在、3条跨类重复（权威/自尊/自由）、1条已存在（权力与权利覆盖）、抑郁症+抑郁合并、强者+弱者合并，实际新增79条（被冒犯在前批已建，过滤掉1条重复）。
- **Cat1 资本职场商业 +18条**（MODERN_BELIEF）：资本家/职业规划/强者与弱者/人设/精致/努力/舒适区/商业/商业思维/品牌/攀比/气质/谦逊/驱动力/情绪价值/趋同/权威/精英
- **Cat2 宏观社科 +13条**（MODERN_BELIEF）：社会实践/社会主义/政治/政权/主权/种族主义/社会治理/舆论/政治正确/战争/制度/阶级/特权
- **Cat3 人际亲密关系 +10条**（DILEMMA 3 + MODERN_BELIEF 7）：社交/社交策略/识人(DILEMMA) + 亲子关系/择偶/早恋/婚姻/婆媳/自尊/人际关系(MODERN_BELIEF)
- **Cat4 劳动科技产业 +10条**（MODERN_BELIEF）：资产/制造业/新能源/芯片/智能制造/智能驾驶/市场/消费/实体经济/劳动异化
- **Cat5 心理哲学道德 +14条**（ACADEMIC 14）：人性/偏见/偏执/理性/自由/欲望/贪婪/痛苦/同情/享乐主义/现实主义/悲观乐观/心理咨询/抑郁
- **Cat6 文化艺术信仰 +14条**（ACADEMIC 4 + IDIOM 3 + MODERN_BELIEF 7）：自然法/哲学/真理/意志(ACADEMIC) + 侠义/忠孝/尊师重道(IDIOM) + 艺术创作/艺术批评/宗教/偶像/英雄/审美观/自强(MODERN_BELIEF)
- 生成脚本分四批：`scripts/add_cat1_full.py`/`add_cat2_cat4_full.py`/`add_cat3_full.py`/`add_cat5_cat6_full.py`，各自生成JSON中间文件后统一合并。
- BUILD SUCCESSFUL，APK 11.9MB，350条字段完整性全部通过。
- 最终分布：MODERN_BELIEF 145 / IDIOM 121 / ACADEMIC 42 / COINED 22 / DILEMMA 20。

## 词条数据质量审计与全量修复
- 审计发现9类问题（4致命+2严重+3中等/轻微），全部集中在最后一批79条新增词条的批量生成脚本格式不一致。
- **JSON数据修复**（`scripts/fix_all_data_issues.py`，修复96条词条340处变更）：
  - townPerspective: list→string（86条，`\n\n`拼接，修复UI `.split("\n\n")` 无法分段导致显示JSON乱码）
  - distortedTruth: list→string（5条）
  - examples: str/dict→list（12条，修复 `optJSONArray()` 返回null导致内容丢失）
  - spotlight: 补weight字段（158个spotlight，修复 `getDouble("weight")` 抛异常导致spotlight被静默跳过）
  - category: 中文枚举→英文枚举名（79条，如"底层认知"→"TOWN_WISDOM"，修复 `valueOf()` 失败默认错误分类）
  - toxicityLevel: 中文枚举→英文枚举名（79条，同上）
  - 额外修复：EMOTION→COGNITION（情绪负债）、HERITAGE→COGNITION/INTERPERSONAL/METHOD（3条旧词条历史遗留无效category值）
- **代码修复**：
  - `IdiomAssetLoader.kt`：新增 cognitiveBiasTags 解析逻辑（之前完全未解析，350条tags运行时全部为空）
  - `Spotlight.kt`：SpotlightCategory 新增6个枚举值（SELF_GROWTH/SOCIAL/CONSUMPTION/WORKPLACE/FAMILY/RELATIONSHIP），修复158个spotlight因category无法valueOf被静默跳过
- BUILD SUCCESSFUL，APK 11.6MB，350条全量验证通过：字段完整性✓ / 类型一致性✓ / 枚举值有效性✓ / cognitiveBiasTags覆盖率88%（309/350有值）。
- 修复脚本可重复执行：`scripts/fix_all_data_issues.py`。

## 显示状态检查（22:59）
- BUILD SUCCESSFUL，APK 12.2MB，350条词条编译通过。
- 代码对空值有安全防护：空spotlight（第592行`isNotEmpty()`判断）不显示警示提示区块、空cognitiveBiasTags（第1016行`isNotEmpty()`判断）不显示标签行，均不会崩溃。
- **已知数据缺陷**（不影响运行，仅内容不完整）：
  - 2条词条spotlight为空：点石成金、长生不老 → 详情页缺"警示提示"区块
  - 41条词条cognitiveBiasTags为空（主要是COINED概括词条+部分传统成语）→ 详情页头部缺"#认知偏差标签"
- **审计脚本误报澄清**：`audit_deep.py`第98行检查`content`字段，但代码和数据实际用`text`字段（348条误报"缺字段"）；第211行正则`唯物史观[：:]`不匹配实际文本"唯物史观视角："（350条误报"无前缀"）。两处均为审计脚本自身bug，数据实际正常。

## 补全43条缺失数据 + 修复段落间距翻倍bug（23:03）
- **数据补全**（`scripts/fix_missing_data.py`）：
  - 2条补spotlight：点石成金（3条COGNITION/CONSUMPTION/INTERPERSONAL）、长生不老（3条COGNITION/CONSUMPTION/EMOTION）
  - 41条补cognitiveBiasTags：从distortedTruth文本提取+按内容人工匹配，涉及TOWN_SYSTEM/COGNITION/INTERPERSONAL/RELATIONSHIP/METHOD/SOCIETY/CHARACTER七类
  - 最终350条spotlight覆盖率100%、cognitiveBiasTags覆盖率100%
- **布局bug修复**（`IdiomCriticScreen.kt` TownPerspectiveContent）：
  - Bug：TownPerspectiveContent内部Spacer(16dp)被外层Column spacedBy(14dp)叠加干扰，段落实际间距=14+16+14=44dp（远超案例区块10dp和风险区块12dp）
  - 修复：内部包裹独立Column(verticalArrangement=Arrangement.spacedBy(14.dp))，隔离外层spacedBy，段落间距统一为14dp
  - 对比区块间距一致性：案例区块10dp / 辩证段落14dp / 风险区块12dp，均在合理范围
- BUILD SUCCESSFUL，APK 12.2MB。

## UI文字间距与布局一致性全面修复（23:08）
- **lineHeight硬编码→Token统一**（6处）：
  - 关于字典简介 `22.sp`→`bodyLineHeight`
  - distortedTruth `22.sp`→`bodyLineHeight`
  - toxicity displayName 缺lineHeight→补`captionLineHeight`
  - toxicity definition/townAttitude `18.sp`→`captionLineHeight`
  - 思辨笔记预览 `14.sp`+`22.sp`→`bodySize`+`bodyLineHeight`
  - IdiomCardItem caption `18.sp`→`captionLineHeight`
- **思辨笔记标题字号统一**：`titleSize`(18sp)+`Bold` → `headingSize`(17sp)+`headingWeight`(Bold token)，与其他模块标题一致
- **思辨笔记Column补fillMaxWidth**：之前缺Modifier导致内部Row/TextField宽度依赖父布局
- **ReadingNavigation间距叠加修复**：`spacedBy(12.dp)`+`Spacer(4.dp)`叠加导致16dp→移除spacedBy改用显式Spacer（4dp/4dp/12dp精确控制）
- **底部文案字号统一**：详情页底部 `15.sp`→`captionSize`(13sp token)，与分类总览底部一致
- **核心启示标题间距统一**：`padding(bottom=10.dp)`→`padding(bottom=12.dp)`，与CollapsibleSection内容区`padding(top=12.dp)`对称
- **SearchResultItem内边距统一**：`12dp`→`14dp`（匹配IdiomCardItem）、标签`2dp`→`3dp`（匹配其他卡片）、fontWeight硬编码→token
- BUILD SUCCESSFUL，APK 12.2MB。
